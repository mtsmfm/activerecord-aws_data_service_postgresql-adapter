source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in app.gemspec
gemspec

gem "pry-byebug"
gem "activerecord", require: "active_record"
